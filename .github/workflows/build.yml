name: Build APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcairo2-dev libgirepository1.0-dev libpango1.0-dev libffi-dev pkg-config build-essential

      - name: Install Briefcase and Python dependencies
        run: |
          pip install briefcase toga
          pip install requests
          pip install avwx-engine
          pip install airportsdata
          pip install airports-py

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK
        run: |
          briefcase create android
          briefcase build android

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: build/android/gradle/app/build/outputs/apk/debug/*.apk
